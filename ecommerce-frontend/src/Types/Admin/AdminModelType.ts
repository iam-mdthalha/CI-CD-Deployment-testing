export interface PrdModelMst {
  crAt: string;
  crBy: string;
  id: number;
  is_active: string;
  plant: string;
  prdModelDesc: string;
  upAt: string;
  upBy: string;
}

export interface Model {
  crAt: string;
  crBy: string;
  id: number;
  is_active: string;
  plant: string;
  prdModelDesc: string;
  upAt: string;
  upBy: string;
}
